#ifndef DEF_ICE_LABEL_PRIVATE
#define DEF_ICE_LABEL_PRIVATE

/**
 * \brief Update the Select structure of label when a realloc is called to keep pointer good
 */
void ICE_Label_SelectUpdate();

#endif